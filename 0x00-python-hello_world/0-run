#!/bin/bash

if [ -z "$PYFILE" ]; then
    echo "PYFILE environment variable is not set. Please export it with the Python script's file name."
    exit 1
fi

if [ ! -f "$PYFILE" ]; then
    echo "Python file '$PYFILE' not found."
    exit 1
fi

python3 "$PYFILE"

